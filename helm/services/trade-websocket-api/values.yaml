# Default overrides for the Trade WebSocket API.
# Adjust container and service ports if the module exposes a different endpoint.

global:
  labels:
    app.kubernetes.io/part-of: stocat
    app.kubernetes.io/component: trade-websocket-api

image:
  repository: stocat/trade-websocket-api
  tag: "0.0.1"

containerPort: 8080

service:
  enabled: true
  port: 8080

env: []

profiles:
  active: ""
